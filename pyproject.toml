[project]
name = "quizapp-api"
version = "0.1.0"
description = "Real-time quiz and flashcard platform for algorithm learning, built with FastAPI, PostgreSQL, and Redis."
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
]
